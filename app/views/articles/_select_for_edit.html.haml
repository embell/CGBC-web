%h3 Edit Article
= form_for @article, method: 'get', url: {action: 'edit', controller: 'articles'} do |f|
  .form-group
    = f.select :id, options_for_select(@all_articles), {}, {class: 'form-control'}
  = f.submit 'Edit', class: 'btn btn-primary'
