.section
  %h1 Delete Newsletter
  = form_for @newsletter, method: :delete do |f|
    .form-group
      = f.select :id, options_for_select(@all_newsletters), {}, {class: 'form-control'}
    = f.submit 'Delete', class: 'btn btn-danger', confirm: 'Are you really sure?'
