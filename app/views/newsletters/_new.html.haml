%h3 Add Newsletter
- @newsletter ||= Newsletter.new
= form_for @newsletter, url: {action: 'create', controller: 'newsletters'} do |f|
  .form-group
    = f.label 'File', nil, class: 'control-label'
    = f.file_field :document, class: 'form-control', required: true
  .form-group
    = f.label 'Date', nil, class: 'control-label'
    .form-group
      = f.date_select :date, {discard_day: true}, {class: 'form-control date-select'}
      .clearfix
  = f.submit 'Add', class: 'btn btn-primary'
