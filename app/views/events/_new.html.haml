.section
  %h1 Add Event
  - @event ||= Event.new
  = form_for @event, url: {action: 'create', controller: 'events'} do |f|
    .form-group
      = f.label 'Name', nil, class: 'control-label'
      = f.text_field :name, class: 'form-control'
    .form-group
      = f.label 'Start Date', nil, class: 'control-label'
      .form-group
        = f.date_select :start_date, {}, {class: 'form-control date-select'}
        .clearfix
    .form-group
      = f.label 'End Date', nil, class: 'control-label'
      .form-group
        = f.date_select :end_date, {include_blank: true, default: nil}, {class: 'form-control date-select'}
        .clearfix
    .form-group
      = f.label 'Description', nil, class: 'control-label'
      = f.text_area :description, rows: 4, class: 'form-control'
    = f.submit 'Add', class: 'btn btn-primary'
